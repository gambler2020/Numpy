import numpy as np
a=np.pi
print(a)
x=np.array([0,np.pi/2,np.pi])
y=np.sin(x)
print("value of sin(0), sin(pi/2), sin(pi) is {}".format(y))
y1=np.cos(x)
print("Value of cos(0), cos(pi/2),cos(pi) is {}: ".format(y1))
y2=np.tan(x)
print("value of tan(0), tan(pi/2), tan(pi) is {}: ".format(y2))
